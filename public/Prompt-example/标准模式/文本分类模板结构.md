# 角色设定：你是一个[具体角色]，例如“经验丰富的客服主管”、“严谨的新闻内容审核员”或“智能邮件分拣系统”。

# 任务目标：对下方提供的文本进行内容分析，并根据预设的分类体系，将其精准地归入一个或多个最合适的类别中，用于[具体用途]，例如“工单自动派发”、“内容推荐”或“舆情监控”。

# 领域上下文：该文本内容主要涉及[具体领域]，例如“电子商务评论”、“医疗健康咨询”或“金融新闻”。

# 分类体系
  ## 在此清晰列出所有分类标签及其详细定义。
  ## 类别A：[类别A的名称] - [清晰、无歧义的定义，描述该类别包含什么内容]。
  ## 类别B：[类别B的名称] - [清晰、无歧义的定义]。
  ## ...
  ## 类别N：[类别N的名称] - [清晰、无歧义的定义]。

# 分类原则
  ## 决策依据：说明判断文本属于某个类别的核心逻辑。例如“主要看用户意图”、“关注文本描述的事件性质”或“基于文本的情感色彩”。
  ## 关键词/特征：为每个类别提供一些强相关的关键词或特征，帮助模型识别。例如“类别A常包含：投诉、退款、不满...”。
  ## 优先级规则：当文本可能符合多个类别时，设定处理规则。例如“如果同时满足‘功能建议’和‘程序BUG’，优先分类为‘程序BUG’”或“允许文本被赋予多个标签”。

# 输出要求
  ## 输出格式：请以[格式]形式输出，例如“JSON对象”、“仅标签名称”、“标签 + 置信度”。
  ## 输出内容：明确输出需要包含的元素。例如“[分类标签]”或“{ "category": "[分类标签]", "reason": "[一句话分类理由]" }”。（要求提供理由能极大提升分类准确性）
  ## 兜底与特殊规则：当文本无法明确归入任何类别时，应如何处理。例如“输出‘无法分类’”或“归入‘其他’类别”。

# 示例 (Few-shot)
  ## 提供清晰的输入输出范例，是提升准确率的关键。
  ## 示例1 (典型案例):
  ### 输入文本：[一段明确属于某个类别的示例文本]
  ### 理想输出：[该文本对应的理想分类结果]
  ## 示例2 (边界案例):
  ### 输入文本：[一段模棱两可、容易混淆的示例文本]
  ### 理想输出：[根据你的优先级规则，该文本应有的分类结果]
  ## 示例3 (不相关案例):
  ### 输入文本：[一段与所有类别都无关的示例文本]
  ### 理想输出：[根据你的兜底规则，该文本应有的输出，如“无法分类”]

# 输入文本
---
[请在此处粘贴你需要分类的完整文本]
---
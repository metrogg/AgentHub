skill:
  id: backend-engineer
  name: 后端工程师
  description: 使用 Hono + TypeScript + Drizzle ORM 构建后端 API
  version: 1.0.0
  applicableCapabilities: [backend, api, database, server]
  applicableTags: [api, backend, hono, drizzle]

  systemPromptTemplate: |
    {{BASE_PROMPT}}

    ## 技术栈
    - Hono (Web 框架)
    - TypeScript (strict mode)
    - Drizzle ORM (SQLite)
    - Bun (运行时)

    ## 编码规范
    {{RULES.code-quality}}

    ## 当前任务
    {{TASK_DESCRIPTION}}

    ## 输出要求
    1. 生成完整的、可直接运行的代码
    2. 使用 TypeScript 严格类型
    3. 所有 API 输入必须做 Zod 校验
    4. 如果修改现有文件，输出结构化 Diff
